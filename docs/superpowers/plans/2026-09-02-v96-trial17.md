# V9.6 Trial 17 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a preregistered V9.6 Trial 17 that validates fresh total FUTSTK OI expansion on a fixed older NSE history window without opening any prior locked final.

**Architecture:** Add a focused `v96_trial17` evidence module and a separate runner/UI path. Reuse NSE contract-history, cash-price, MWPL, cluster-bootstrap, and regression primitives; do not reuse the V9.5 discovery partition as Trial 17 evidence.

**Tech Stack:** Python, pandas, NumPy, FastAPI/HTML+JS, pytest.

**Spec:** `docs/superpowers/specs/2026-09-02-v96-trial17-design.md`

## Global Constraints
- Event threshold is frozen at total OI z >= 1.5.
- Independent evidence dates are 2021-09-01 through 2023-09-01 inclusive.
- 2D cannot rescue 1D.
- Trial 15 and all prior finals remain locked.
- `ACTIVE_PLAYBOOKS = ()` remains unchanged.
- Trial 17 is research/shadow only.

---

### Task 1: Trial 17 evidence evaluator
**Files:**
- Create: `app/v96_trial17.py`
- Test: `tests/test_v960_trial17.py`

**Interfaces:**
- Consumes: symbol frames containing `nse_total_oi`, cash OHLC-derived outcomes, DTE, membership, and optional MWPL/ban.
- Produces: `trial17_spec()` and `evaluate_trial17(frames, controls, bootstrap_reps=...) -> dict`.

- [ ] Write tests proving the fixed dates/threshold, final-lock semantics, pass/fail hierarchy, 1D/2D rule, clustered lift, t-stat gate, top-3-day sensitivity, chronological blocks, DTE diagnostics, and concentration diagnostics.
- [ ] Run the tests and verify RED because `app.v96_trial17` does not exist.
- [ ] Implement the minimum evaluator using existing V9.5 statistical helpers.
- [ ] Run the focused tests and verify GREEN.

### Task 2: Independent NSE history runner
**Files:**
- Modify: `app/backtest.py`
- Test: `tests/test_v960_runner.py`

**Interfaces:**
- Produces: `run_v96_trial17(kite, progress_cb=None, stage_cb=None, integrity_data=None, resume_run_dir=None)`.

- [ ] Write tests proving the runner requests warm-up history before 2021-09-01, filters evidence to <=2023-09-01, uses official NSE total OI, discovers point-in-time FUTSTK membership, and never calls V9.5/Trial-15 final data.
- [ ] Verify RED.
- [ ] Implement the independent runner with durable NSE cache reuse and validation-window MWPL loading.
- [ ] Verify GREEN.

### Task 3: API/state/UI isolation
**Files:**
- Modify: `app/backtest.py`
- Modify: `app/web.py`
- Test: `tests/test_v960_ui.py`

**Interfaces:**
- Separate V9.6 state and run endpoint; no implicit V9.5/V9.4 execution.

- [ ] Write failing tests for V9.6 build marker, Trial 17 card, fixed date disclosure, research-only copy, and Trial 18 lock.
- [ ] Implement endpoint/state/UI.
- [ ] Verify focused UI tests.

### Task 4: Release safety and documentation
**Files:**
- Modify: `README.md`
- Modify: `PRODUCTION_BUILD.txt`
- Modify: `RESEARCH_BUILD.txt`
- Create: `V9_6_CHANGELOG.md`
- Test: `tests/test_v960_release.py`

**Interfaces:**
- Build id: `2026-09-02-INSTITUTIONAL-V9.6.0-TRIAL17-INDEPENDENT-TOTAL-OI`.

- [ ] Write failing release tests for build markers, frozen threshold/dates, locked prior finals, Trial 18 lock, and empty production playbooks.
- [ ] Update docs/markers.
- [ ] Verify focused release tests.
- [ ] Run the full test suite, Python compilation, and rendered JS syntax checks.
- [ ] Package without secrets/runtime caches.
- [ ] Extract the exact ZIP into a clean directory and rerun the full verification.
