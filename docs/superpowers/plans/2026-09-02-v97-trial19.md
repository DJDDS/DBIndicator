# V9.7 Trial 19 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a preregistered nonlinear Trial 19 on 2018-09-01 through 2021-08-31 using same-day plus same-DTE controls and binary-event two-way clustered inference.

**Architecture:** Add `app/v97_trial19.py` for frozen math and verdicts, a separate V9.7 NSE archive runner/state/API in `app/backtest.py` and `app/web.py`, and a V9.7 primary card in `app/templates/backtest.html`. Reuse existing official NSE futures/cash/MWPL/earnings clients without changing V9.6.2.

**Tech Stack:** Python, pandas, NumPy, Flask, existing NSE archive clients, pytest, vanilla browser JavaScript.

**Spec:** `docs/superpowers/specs/2026-09-02-v97-trial19-design.md`

## Global Constraints
- Threshold remains exactly `total_z >= 1.5`.
- Trial-19 evidence window is exactly 2018-09-01 through 2021-08-31.
- No V9.5/V9.6 final or locked partition is opened.
- Trial 18 remains locked unless Trial 19 and its earnings promotion control pass; never auto-run.
- `ACTIVE_PLAYBOOKS = ()` remains unchanged.

---

### Task 1: Trial-19 nonlinear evidence module
**Files:** Create `app/v97_trial19.py`; Test `tests/test_v970_trial19.py`.
**Interfaces:** Produces `trial19_spec()`, `trial18_spec()`, `same_day_dte_matched_report()`, `evaluate_trial19()`.
- [ ] Write failing tests for frozen dates/threshold, matched baseline exclusion, binary-event two-way t-stat, fail-closed integrity and pass gates.
- [ ] Run focused tests and confirm RED.
- [ ] Implement minimal module reusing V9.6.2 robust primitives where valid.
- [ ] Run focused tests and confirm GREEN.

### Task 2: Independent older NSE runner
**Files:** Modify `app/backtest.py`; Test `tests/test_v970_runner.py`.
**Interfaces:** Produces `run_v97_trial19`, `start_v97_trial19`, `get_v97_trial19_state`.
- [ ] Write failing runner tests using supplied synthetic futures/cash/MWPL/earnings inputs.
- [ ] Confirm RED.
- [ ] Fetch warm-up plus 2018-2021 archive via existing NSE clients, discover historical membership, build point-in-time frames, add MWPL, evaluate Trial 19, then earnings promotion only after a Trial-19 PASS.
- [ ] Confirm GREEN.

### Task 3: API/UI and release safety
**Files:** Modify `app/web.py`, `app/templates/backtest.html`, `README.md`, `RESEARCH_BUILD.txt`, `PRODUCTION_BUILD.txt`; Create `V9_7_CHANGELOG.md`; Test `tests/test_v970_ui_release.py`.
**Interfaces:** `/api/v97/run`, `/api/v97/status`; V9.7 card and status renderer.
- [ ] Write failing UI/release tests.
- [ ] Confirm RED.
- [ ] Add V9.7 primary card, keep V9.6.2 visible as prior closed research, update build markers, preserve no-production assertions.
- [ ] Confirm GREEN.

### Task 4: Full verification and clean archive
**Files:** Entire repository and final ZIP.
- [ ] Run complete pytest coverage.
- [ ] Compile Python.
- [ ] Validate rendered browser JavaScript syntax.
- [ ] Package excluding `.env`, runtime state and caches.
- [ ] Extract exact ZIP to a clean directory and repeat verification.
